import eventBus from 'eventbus-es';

// console.log('1', +new Date());
setTimeout(function() {
	// console.log('2', +new Date());
	eventBus.$emit('handler');
	eventBus.$emit('handler2');
}, 1000);

setTimeout(function() {
	// console.log('3', +new Date());
	eventBus.$on('handler', function() {
		console.log('this is handler');
	});
	eventBus.$on('handler1', function() {
		console.log('this is handler');
	});
}, 1200);

// console.log(Array.from(new Set([1, 2, 1, 3, 2])));

// console.log([1, 2, 3, 4].findIndex(n => n === 4));

// console.log(Object.assign({}, {name: 12341}, {year: 21314}));